import { breakPoints, bodies, fontSizes } from '../../../common/styles/variables'

const headerNormal = `

`

const headerDesktop = `

`

export const bannerStyle = `${headerNormal}${headerDesktop}`

export default {
    bannerStyle,
}
